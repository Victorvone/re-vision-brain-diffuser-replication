# original_repo and working_repo are their own separately-tracked git repositories
# (working_repo has its own git history, cloned from original_repo). Not nested here
# to avoid submodule/gitlink confusion; each is tracked independently.
original_repo/
working_repo/

# local Claude Code session state, not project content
.claude/
